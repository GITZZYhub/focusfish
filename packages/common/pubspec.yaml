name: common
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # 自定义包
  getx:
    path: ../getx # getX包
  my_logger:
    path: ../my_logger # 日志打印工具
  dio_http:
    path: ../dio_http # 网络请求
  localization:
    path: ../localization # 国际化
  resources:
    path: ../resources #资源包
  permissions:
    path: ../permissions # 权限申请
  flukit:
    path: ../flukit # 自定义组件
  moor_db:
    path: ../moor_db # 数据库
  eventbus:
    path: ../eventbus #eventbus包
  my_sentry:
    path: ../my_sentry # 错误日志收集工具

  #自定义插件
  native_string:
    path: ../../native_string # 字符加密
  sensor_plugin:
    path: ../../sensor_plugin # 传感器插件
  vibrate_plugin:
    path: ../../vibrate_plugin # 震动插件

  #第三方库
  shared_preferences: ^2.0.13
  json_annotation: ^4.4.0 # json
  lpinyin: ^2.0.3 #汉子转拼音
  pausable_timer: ^1.0.0+4 #可暂停的定时器
  fl_chart: ^0.46.0 #图表

dev_dependencies:
  build_runner: ^2.1.8
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  json_serializable: ^6.1.5

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # To add assets to your package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/assets-and-images/#from-packages
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # To add custom fonts to your package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/custom-fonts/#from-packages
